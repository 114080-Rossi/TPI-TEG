import { Card } from './Card';
import { Player } from './Player';
import { Board } from './Board';

export class CountryCard extends Card {
  applyEffect(player: Player, board: Board): void {
    // lógica de carta de país
  }
}
